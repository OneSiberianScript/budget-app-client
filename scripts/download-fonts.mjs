#!/usr/bin/env node
/**
 * Optional: download custom font files into public/fonts.
 * See src/app/styles/fonts.css for @font-face declarations.
 */
console.log('No fonts configured. Add URLs and output paths if needed.')
