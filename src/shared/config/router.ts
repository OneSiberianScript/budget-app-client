export const ROUTE_NAMES = {
    LOGIN: 'login',
    REGISTER: 'register',
    HOME: 'home'
} as const

export const ROUTE_PATHS = {
    LOGIN: '/auth/login',
    REGISTER: '/auth/register',
    HOME: '/'
} as const
