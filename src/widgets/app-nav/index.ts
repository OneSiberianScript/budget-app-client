/**
 * Главная навигация приложения: на мобиле — фиксированная панель внизу с иконками и safe area,
 * на десктопе (≥768px) — верхняя панель над шапкой с иконками и подписями. Подсвечивает текущий раздел и вложенные маршруты.
 */
export { default as AppNav } from './ui/AppNav.vue'
